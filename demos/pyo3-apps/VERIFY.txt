== 1. READ path (pure Rust, no Python): list CRM Deals ==
  PASS: GET list returns data
== 2. WRITE path (Python controller lifecycle): create CRM Deal ==
  PASS: POST creates a named CRM Deal
== 3. CONTROLLER VALIDATE runs (hrms RetentionBonus throws on past date) ==
  PASS: past date -> controller frappe.throw (417)
  PASS: exact controller message surfaced
== 4. RUST validation still applies after controller passes (mandatory field) ==
  PASS: future date passes controller, Rust catches missing 'company'
== 5. selective dispatch: read never enters Python (timing/registry) ==
  PASS: ToDo (frappe-core, pure-CRUD) read works in Rust
== 6. CHILD TABLES persist through the Python dispatch path (was a data-loss bug) ==
  PASS: 2 child rows persisted (before=2 after=4)
== 7. NestedSet/WebsiteGenerator doctypes now REGISTER (run their own logic) ==
  PASS: CRM Sales Hierarchy (NestedSet) is served

RESULT: 8 passed, 0 failed
ALL VERIFIED — the 5 apps run on ferro (reads in Rust, writes drive real controller Python).
